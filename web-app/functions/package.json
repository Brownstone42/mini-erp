{
  "name": "mini-erp-functions",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "eslint src",
    "test": "node --test",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "csv-parse": "^7.0.2",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^7.1.0",
    "iconv-lite": "^0.7.3"
  }
}
